@model HomePageViewModel
@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="mb-5 shadow-sm">
    <partial name="_Slider" model="Model.Sliders"></partial>
</div>

<section class="py-5">
    <div class="container">
        
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold m-0">Öne Çıkan Ürünler</h2>
            <a href="/Products" class="btn btn-outline-primary btn-sm rounded-pill px-4">
                Tümünü Gör <i class="bi bi-arrow-right ms-1"></i>
            </a>
        </div>
        
        <hr class="mb-5 text-muted opacity-25">

        <div class="row gx-4 gx-lg-5 row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
            @if (Model.Products != null && Model.Products.Any())
            {
                @foreach (var item in Model.Products)
                {
                    <partial name="_ProductView" model="item"></partial>
                }
            }
        </div>
                    <div class="row gx-4 gx-lg-5 row-cols-1 row-cols-md-2 justify-content-center">
                @if (Model.News != null && Model.News.Any())
                {
                    @foreach (var item in Model.News)
                    {
                        <partial name="_NewsView" model="item"></partial>
                    }
                }
                else
                {
                    <div class="col-12 text-center">
                        <div class="alert alert-light border shadow-sm">
                            Henüz aktif bir kampanya bulunmuyor.
                        </div>
                    </div>
                }
            </div>
    </div>
</section>